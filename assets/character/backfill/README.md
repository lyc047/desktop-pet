# AI 补全底图

这里保存为分层动画补齐遮挡区而生成的透明底图；它们不会替换 `pet.png`，也不会覆盖 `layers/` 里的原始拆件。

命名约定：

- `head_under_hair_v1.png`：前发下方的头部、耳朵和颈部；当前是实验件，未通过位置对齐检查，暂不接入。
- `torso_under_limbs_v1.png`：手臂下方的连贯卫衣与短裤。
- `hair_back_complete_v1.png`：脸部和前发后方的完整后发。

所有文件须为 1086 × 1448、RGBA、透明背景。只有通过叠放预览检查后，才可接入运行时动画。
