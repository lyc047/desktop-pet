# 更新日志 Changelog

记录桌宠各版本的功能变化。每个版本对比其上一个版本。

---

## [2026-08-30] 手绘小屋 + 剪贴板历史

> 提交 `9ea1850`。核心是新增了一个**可交互的手绘小屋**，以及**剪贴板历史**工具。小屋功能为主要改动，由独立模块实现，未影响原有桌宠行为。

### 新增：手绘小屋（核心功能）

右键桌宠选择“回家”，桌宠会暂时离开桌面，进入一个程序绘制的手绘小房间。房间里的物件大部分可以点击互动：

- **阅读与书架**
  - 翻开的书 → 打开“读书器”，可导入章节、翻页阅读（支持在收纳箱的小屋书架目录中管理书籍）。
  - 书架 → 打开书架管理，可导入/删除/切换书籍。
- **桌面小物件**（点击触发短动画）
  - 杯子倒水（水流 + 蒸汽）、翻书页、抽屉/小抽屉开合、玩具车滑动、靠垫按压回弹、窗帘摆动、挂画扶正、台灯开光晕、收音机音符、窗边植物与吊兰摇摆、便签打开桌宠便签。
- **互动工具**
  - 数字华容道 → 打开 4×4 可玩的小游戏。
  - 小画板 → 打开简易绘画窗口，可保存 PNG 画作。
- **小屋出口**
  - 收纳箱 → 打开桌宠收纳目录。
  - 沙发 → 回到桌面并进入睡眠状态。
  - 房门 → 离开小屋，回到桌面。

小屋基于 `house_scene_refined.py`（`HandDrawnHouse`）实现，`main.py` 从 `house_window.py` 导入（该文件作为入口转发到 refined 实现）。采用“底图 + 独立透明物件层 + 代码绘制动画”的分层方案：房间底图只含固定结构（`room_base_master_v2.png`），每个可交互物件是独立透明 PNG（`objects/refined/`），各自持有动画状态，可并行动画；华容道与画板对话框由 `house_scene_v2.py` 提供。

### 新增：剪贴板历史

右键桌宠选择“剪贴板历史（最近 5 条）”，自动记录最近 5 条文字、链接、图片或文件：

- 查看类型、时间与内容摘要；图片显示缩略图，多文件列出文件名。
- 每条记录可单独复制回剪贴板或删除，也可一键清空。
- 历史保存在 `%APPDATA%/DesktopPet/`，重启桌宠后仍保留。
- 复制回剪贴板后桌宠会说话提示。

### 新增：远程图片下载

拖入图片链接时自动尝试下载图片并保存到图片收藏（`_download_remote_images`），替代原先仅保存链接。

### 新增：睡觉中的接收反馈动画

睡眠状态下拖入内容时，会播放“梦境接收”动画与台词（`dream_receive`），与清醒时接收区分开。

### 其他调整

- 设置界面新增两组台词：睡梦中接收、从剪贴板历史复制。
- 高级设置“打扰限制”默认值由 4 调整为 6，旧配置自动迁移。
- `settings_dialog.py`、`main.py` 相应扩展；README 补充剪贴板历史与小屋说明。

### 文件与素材

- 新增：`house_window.py`、`house_scene_v2.py`、`house_scene_refined.py`、`reader_dialog.py`、`clipboard_history.py`
- 新增素材：`assets/house/` 下的小屋背景、独立物件（`objects/refined/`）、茶具动画（`objects/tea_v2/`）、桌宠位置素材（`pet_positions/`）等
- 修改：`main.py`、`settings_dialog.py`、`README.md`、`.gitignore`
